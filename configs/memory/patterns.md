# Agent Memory — Patterns (o que funciona)

> Padrões reutilizáveis e preferências do usuário que já funcionaram.
> Formato: ✅ Quando <situação>, faça <o que funciona>. Entradas curtas e acionáveis.
> Processos reutilizáveis são promovidos a SKILLS (ver `code-playbooks/references/skill-authoring.md`) e removidos daqui — sem redundância.

## Padrões / Preferências (o que funciona)

<!-- - 2026-08-20 ✅ Quando <situação>, faça <o que funciona> -->
- 2026-08-21 ✅ Quando precisar cadastrar/consultar servidores SSH, consulte `~/.config/opencode/extras/ssh_servers.md` (inventário local) ou `ssh-manager server list` — nunca IPs/chaves no código (porque mantém dados privados fora do versionamento).
- 2026-08-21 ✅ Quando precisar criar arquivos temporários de teste para bibliotecas com file handler próprio (`xlsxwriter`, `openpyxl`, `sqlite3`), use `with tempfile.TemporaryDirectory() as tmpdir:` (porque garante limpeza automática cross-platform sem locks no Windows e passa no Ruff `SIM115`).
- 2026-08-21 ✅ Quando rodar type-check no frontend, execute `npm --prefix frontend run type-check` (ou `npm ci` seguido de `npm run type-check`) para utilizar o TypeScript das devDependencies (porque evita downloads espúrios do npm).
- 2026-08-21 ✅ Quando persistir contexto/estado incremental em PostgreSQL (JSONB), use merge não-destrutivo `COALESCE(context_data, '{}'::jsonb) || $2::jsonb` no `ON CONFLICT DO UPDATE` (porque impede que nós de auto-save periódicos apaguem campos detalhados coletados por tools anteriores).
- 2026-08-21 ✅ Quando implementar tools com efeitos colaterais externos (Google Calendar, criação de pedidos, envio de e-mails), estruture protocolo de 2 fases no System Prompt (Fase 1: Propor e solicitar confirmação explícita; Fase 2: Só executar a tool após resposta afirmativa do usuário).
- 2026-08-24 (rev. 2026-09-22: forma nativa V2 — `mcp.servers` + `disabled` + `timeout: {catalog, execution}`) ✅ Quando instalar MCP opcional no opencode, adicione entrada no config ÚNICO `~/.config/opencode/opencode.json` (não existe mais `.jsonc` desde 2026-08-26) com `type: local`, `["bunx", "<pkg>@<versão>", ...]` (versão pinada — nunca `npx -y @latest`, ver lição 2026-08-29), `"timeout": {"catalog": 30000, "execution": 30000}` e `"disabled": false` (porque arquivos de config são mesclados por chave com arrays conflitantes; o timeout default de 5s estoura no primeiro download; disabled por default evita custo de contexto de tools não usadas).
- 2026-08-24 (rev. 2026-09-17) ✅ Quando configurar browser automation, use dois trilhos: MCP `chrome-devtools` (`--no-usage-statistics`, `disabled: true`, opt-in via `/mcp`) para o interativo (o agente escolhe quando usar; 2FA manual, inspeção ao vivo) + `pw` no shell (wrapper versionado `~/.local/bin/pw.cjs` sobre `@playwright/cli` via volta; `--headed` para acompanhar, headless em VPS) para o determinístico (skills `web-dashboard-automation`, `playwright-prod-regression`). ❌ Sem MCP `@playwright/mcp` (removido: duplicava o chrome-devtools e é token-heavy frente ao CLI). ❌ Sem `playwright-cli` cru no shell do agente no Windows (trava — usar `pw`).
- 2026-08-24 (rev. 2026-09-22: INERTE no v2 — runtime ignora o bloco `lsp`; removido dos configs globais. Template guardado p/ quando o runtime voltar) ✅ Quando precisar de LSP de framework/stack específica (ex.: tailwindcss), registre PER-PROJECT no `<projeto>/opencode.json` e não global (porque opencode LSP não tem root_dir/root_markers — inicia por extensão em TODOS os projetos; evidência real: Zed #51469, tailwind LS a 100% CPU em projeto sem Tailwind). Template: `"lsp": {"tailwindcss": {"command": ["tailwindcss-language-server", "--stdio"], "extensions": [".tsx", ".ts", ".css", ".html"]}}`.
- 2026-08-24 (rev. 2026-09-22: VERIFICADO no binário v2.0.8 — `instructions` é campo suportado (`Config.Instructions`, "Additional instruction files or patterns to include") e `experimental.subagent_depth` é lido pelo subagent tool (`??1`, erro orienta aumentar); ambos presentes nos configs) ✅ Quando quiser instruções anti-hang de shell não-interativo, use `"instructions": ["https://raw.githubusercontent.com/JRedeker/opencode-shell-strategy/trunk/shell_strategy.md"]` no opencode.json (porque é um arquivo de instruções remoto, não plugin npm).
- 2026-08-26 (rev. 2026-09-19/22: dcp+ponytail REMOVIDOS — quebram o boot do v2; `dcp.jsonc` removido do provisioning em 2026-09-22 por YAGNI) ✅ Quando sugerir plugins opencode, o instalado e aprovado é APENAS: **@prevalentware/opencode-goal-plugin** (goal mode: tools `get_goal`/`set_goal`/`update_goal`, slash `/goal`, sidebar). Removidos por evidência de não-funcionamento: thinking-fix (no-op: `isReasoningModel:false` em 300+ inspects no modelo deepseek-v4-flash), opencode-visual-cache (TUI cosmético, nunca exibiu dado) e @vymalo/opencode-models-info (inerte: nenhum provider com `modelsInfoUrl`). Descartados por decisão do usuário: auth plugins (sem assinaturas GitLab/ChatGPT/Claude Max), qualquer notifier (nenhum oficial/maduro p/ Windows), oh-my-opencode-slim (risco custo paralelo), skills-collection (conflito c/ skills próprios).
- 2026-08-24 (rev. 2026-09-22: chave nativa agora é `plugins`) ✅ Plugins de funcionalidade: **@prevalentware/opencode-goal-plugin** (`/goal <objetivo>` c/ guardas: evidência p/ fechar, budgets, checkpoints; NÃO cria toggle no Tab — é slash command + tools + indicador sidebar). Array completo vive SOMENTE em `opencode.json` único.
- 2026-08-26 ✅ Quando o shell do OpenCode (Windows) precisar de PowerShell, configure `"shell": "pwsh"` (não caminho fixo) (porque a instalação pode vir do Microsoft Store — `WindowsApps\Microsoft.PowerShell_*` — ou MSI — `Program Files\PowerShell\7`; PATH resolve ambos).
- 2026-08-26 ✅ Quando agentes precisarem de libs globais no Windows, use: Node via `NODE_PATH=%USERPROFILE%\node_modules` (`axios`, `cheerio`, `papaparse`) e Python global (`pyyaml`, `requests`, `openpyxl`, `beautifulsoup4`, `pypdf`, `python-docx`, `lxml`, `sqlite3` stdlib) — SEM venv/node_modules por projeto no Windows; Linux segue isolado (uv/venv). CLIs: `jq`, `dust` (du NTFS-safe), `hyperfine`, `shellcheck`, `bun` (`bunx` > `npx`).
- 2026-08-24 ✅ Quando precisar de code review estruturado, use o agente primário **review** (definido no `opencode.json` do envctl — NÃO existe mais `agents/review.md`; o provisioning remove o dir stale `~/.config/opencode/agents`): read-only (bash granular read-only para evidências, edit deny), severidades BLOCKER/MAJOR/MINOR/NIT (nit só se pedido), evidência obrigatória file:line, YAGNI check contra callers reais, lente de segurança sempre, veredito APPROVE/REQUEST-CHANGES.
- 2026-08-24 ✅ Quando pesquisar agentes/plugins da comunidade opencode, aplique o critério do usuário "oficial ou maduro": packs populares estavam todos imaturos (agents-collection 2★; agentic 622★ mas stale 11 meses; darrenhinde morto). Agentes nativos próprios (definidos no `opencode.json` do envctl) > packs de terceiros.
- 2026-09-03 (rev. 2026-09-22: forma nativa V2 — customs SÓ com IDs novos, nunca override de built-in (`plan` sem `mode`, preserva o original); `permissions[]` com `shell`/`subagent`, `system`, `request.body.temperature`) ✅ Quando configurar agente opencode de planejamento/revisão: `bash` granular `"*": "ask"` + allow list read-only auto (git log/status/diff/show/grep, rg, grep, head, tail, wc, sort, uniq, awk, sed, go test/vet, pytest, npm test, dotnet, cargo test e filtros PowerShell Select-Object/Where-Object/Select-String/Measure-Object/Sort-Object/Group-Object/Out-String/Format-Table) — read-only roda sem aprovação, DEMAIS comandos (node, python, docker...) pedem aprovação (ask) em vez de falhar com "tool negada"; `task` virou `subagent: "allow"` (pode despachar explore/general); `edit` negado ou allow pattern apenas para o dir de artefatos; `todowrite`/`skill` allow; `temperature: 0.1` em `request.body`; `steps` cap (25); `system` mandando carregar `agent-memory` + skill de domínio (`writing-plans`/`verification-before-completion`) e usar context7 — evolução validada em v1.1.44→v1.1.47 (deny total gerava "tool negada" no Review; ask + task allow eliminou o erro; fontes: docs opencode + `permission/arity.ts`).
- 2026-09-13 ✅ Quando configurar agentes customizados do opencode, mantenha APENAS `review` e `plan` (ambos primary; `plan` no **default built-in** — sem `"mode": "all"`, ou seja, não dispatchável via task tool). O agente `goal` (YOLO) foi REMOVIDO por ser redundante com o `build` (prompt "seja autônomo" não é capacidade nova); o plugin `@prevalentware/opencode-goal-plugin` permanece porque o `/goal` funciona a partir de qualquer agente — não recriar o agente goal.
- 2026-09-13 ✅ Quando a tarefa exigir explorar/pesquisar/debugar, delegue proativamente conforme a skill `subagent-routing` (referenciada no AGENTS.md): `explore` para varredura de código sem alvo, `general` para pesquisa/multi-passo; paralelize domínios independentes na MESMA resposta (preserva o contexto do coordenador); **NÃO** paralelize falhas relacionadas/estado compartilhado/debug exploratório. Mecânica e orquestração no mesmo repo: `subagent-routing`.
- 2026-09-13 (rev. 2026-09-22: DCP REMOVIDO do provisioning por YAGNI — plugin V1 quebra o boot do v2 e o `dcp.jsonc` ficou sem consumidor; pruning agora é o compaction nativo) ✅ DCP (`dcp.jsonc`) priorizando **cache-hit**: banda **90%/80%** (limiar alto = menos compressões = cada compressão invalida o prefixo do prompt), `nudgeFrequency: 10` + `iterationNudgeThreshold: 30` (menos injeções de nudge = menos churn de prefixo), `nudgeForce: soft`. Compressão **manual** continua permitida pela tool `compress` (já exposta com `permission: "allow"`) — o AGENTS.md instrui a comprimir proativamente em troca brusca de assunto / fim de sub-tarefa; **não** ligar `manualMode` (desativa a compressão autônoma); `allowSubAgents: true` para DCP rodar também em subagentes. Se o objetivo fosse custo/tokens (não cache), a banda seria baixa.
- 2026-09-19 (rev. 2026-09-22: `dcp.jsonc` REMOVIDO do provisioning — sem consumidor, YAGNI; re-adicionar config + plugin juntos quando o upstream migrar) ✅ Quando um plugin opencode falhar com `PluginModule.LoadError: Expected object at ["default"]` no opencode v2, REMOVA do array `plugins` em `configs/opencode.json` + `configs/opencode.linux.json` em vez de corrigir localmente (a causa é export V1 `async (ctx) => {...}` em vez de `Plugin.define({id, setup})`; cache em `~/.cache/opencode/npm` é recriado a cada boot; atualizar não resolve quando o latest já é o quebrado — dcp 3.1.15, ponytail 4.10.0 em 2026-09-19; referência funcional: `@prevalentware/opencode-goal-plugin`). Abrir issue upstream citando https://opencode.ai/v2/docs/build/plugins/migrate-v1.