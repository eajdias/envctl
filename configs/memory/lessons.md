# Agent Memory — Lessons (não repetir)

> Consulte antes de cada tarefa; atualize a cada correção/aprendizado.
> Formato: ❌ <não faça> → ✅ <faça> (porque). Entradas curtas e acionáveis.
> Processos reutilizáveis são promovidos a SKILLS (skill `memory-promotion`) e removidos daqui — sem redundância.

## Erros / Lições (não repetir)

- 2026-08-26 ✅ **chrome-devtools MCP x Playwright MCP**: o chrome-devtools MCP requer o executável "Google Chrome stable" (não encontrado — máquina tem Edge/Chromium do Playwright) → usar o Playwright MCP para sessões persistentes. No Playwright MCP: `run_code_unsafe` roda na PÁGINA (sem `require`); `filename` deve estar nos allowed roots (workspace ou `.playwright-mcp/`); para executar código Node com fs, gerar o script no workspace e passar o filename.
- 2026-08-26 ❌ Gravar lições sobre a PLATAFORMA/API específica de um projeto (ex.: MessageBird/Flow Builder — formato de steps, retry de webhooks, limites da conta) na memória global → ✅ Essas lições vão EXCLUSIVAMENTE para `.opencode/memory/lessons.md` do projeto (porque a stack/fornecedor é domínio do projeto — não serve para outros projetos e polui a memória global; lá só padrões de engenharia reutilizáveis). Extensão da regra de 2026-08-21 sobre PII: dados de DOMÍNIO incluem APIs/formatos/limites do fornecedor.
- 2026-08-25 ❌ Passar datas como `str` em params de queries asyncpg para colunas `date`/`timestamp` → ✅ Converter com `date.fromisoformat()`/`datetime` antes de `pool.fetch_all` (porque asyncpg levanta `DataError` silencioso e o endpoint cai num fallback raw com labels/dados errados em vez de falhar visível).
- 2026-08-25 ❌ Filtrar buckets com `hasattr(bucket, "date")` para validar range de `datetime.date` → ✅ Usar `hasattr(bucket, "year")` ou comparar direto (`bucket < req_start`) (porque `datetime.date` NÃO tem método `.date()` — isso existe só em `datetime.datetime`; o filtro vira código morto silencioso).
- 2026-08-25 ❌ Assumir que migration criada no repo foi aplicada → ✅ Conferir a lista real de migrations do runner (`_init_schema`) e a tabela `schema_migrations` no banco (porque arquivos de migration que não estão na lista do aplicador NUNCA rodam — foi o caso da 012 que corrigia os rollups e ficou sem aplicar).
- 2026-08-25 ❌ Rodar CLI com subcomando que dispara fluxo completo (ex.: `sync --backfill-incomplete` que cai no full sync varrendo 67k registros) → ✅ Para job pontual, chamar a função de domínio direto via script inline (`python -u` + `PYTHONPATH` do app) (porque CLIs genéricas entram em modos pesados não óbvios; a função direta faz só o necessário).
- 2026-08-25 ❌ Confiar em filtros de data/paginação de APIs de mensageria sem testar contra a API viva → ✅ Sempre validar paginação/filtros com `totalCount` + múltiplas páginas por offset (porque APIs de mensageria podem ignorar filtros de data na listagem e não retornar token de paginação — deploy com paginação por cursor travou em 20 msgs sem erro registrado).
- 2026-08-25 ❌ Assumir que rollup agregado por `created` alinha com painel que filtra `created OR updated` → ✅ Definir a semântica de "data do evento" (updated para avaliações/surveys que chegam dias depois) e agregar por ela em TODAS as camadas (rollup SQL, queries, endpoints) (porque métricas com atraso de origem nunca aparecem no bucket do dia em que ocorreram).
- 2026-08-25 ❌ Confiar no CORS do `.env`/compose sem verificar o container → ✅ Checar o valor real no container (`docker exec <svc> sh -c 'echo $CORS_ORIGINS'`) (porque `environment` do compose prevalece sobre `env_file`; o domínio público novo faltava e o browser falhava com `net::ERR_FAILED` no POST de login).
- 2026-08-21 ❌ Versionar dados de servidores (IPs, usuários, caminhos de chaves) em skills/README/docs do repo → ✅ Manter inventário APENAS em arquivo local por máquina (`~/.config/opencode/extras/ssh_servers.md`); no repo use referência dinâmica (`ssh-manager server list` / MCP `ssh_list_servers`) (porque a skill é deployada via envctl para todas as máquinas e o histórico do git guarda PII para sempre).
- 2026-08-21 ❌ Usar `tempfile.NamedTemporaryFile` mantendo o arquivo aberto no context manager com `xlsxwriter.Workbook`/`openpyxl` e tentar `os.unlink` dentro do bloco no Windows → ✅ Usar `with tempfile.TemporaryDirectory() as tmpdir:` e criar o arquivo dentro do diretório temporário (porque no Windows o handle aberto gera `PermissionError: [WinError 32]` ao tentar remover e `TemporaryDirectory` respeita a regra Ruff `SIM115`).
- 2026-08-21 ❌ Usar `npx tsc` para type-check de TypeScript → ✅ Usar `npm run type-check` ou `npx tsc --noEmit` referenciando devDependencies após `npm ci` no frontend (porque `npx tsc` sem pacote local tenta instalar o pacote deprecated `tsc` do npm em vez do compilador oficial `typescript`).
- 2026-08-21 ❌ Gravar regras de negócio de clientes ou dados de domínio privados (telefones de contato específicos, nomes de produtos do cliente, tokens) na memória global (`~/.config/opencode/memory/`) → ✅ Gravar dados de domínio EXCLUSIVAMENTE na memória do projeto (`.opencode/memory/`); na memória global grave apenas padrões de engenharia, arquitetura e preferências técnicas reutilizáveis (porque mantém segregação limpa de domínio e não vaza PII entre projetos).
- 2026-08-21 ❌ Remover nós do DOM gerenciados por React (portais, overlays de tour) com `el.remove()` durante automação de UI → ✅ Clicar nos botões de dismiss nativos ("Skip", "Got it", "Close") (porque o React lança `NotFoundError: removeChild` no reconciliation e quebra a aplicação inteira).
- 2026-08-23 ❌ Ler `process.env` em config de módulo com `as const` (avalia na importação, antes dos `beforeAll` dos testes) → ✅ Usar **getters lazy** (`get databaseUrl() { return process.env.X ?? fallback }`) (porque permite override por env nos testes e mantém a config viva no momento do acesso).
- 2026-08-23 ❌ Versionar configs/maps de sistemas legados "por precaução" para um futuro incerto → ✅ Versionar APENAS o que o pipeline atual consome; adicionar sob demanda quando a necessidade surgir (porque segue KISS/YAGNI — o restante é código morto; o usuário: "se não são consumidos = código morto... não há necessidade de se resguardar para um futuro que talvez nunca venha").
- 2026-08-24 ❌ Assumir que plugins listados no ecosystem do opencode são pacotes npm (`opencode-notificator`, `opencode-shell-strategy`, `opencode-notifier` deram 404) → ✅ Rodar `npm view <pkg>` ANTES de gravar no config; instalar só o que for oficial/maduro e suportar Windows (notificator panta82 é macOS/Linux only; notifier mohak34 foi unpublishado; usuário: "não tem oficial ou maduro" → não instalar).
- 2026-08-24 ❌ Assumir que ARRAYS em configs `.json`/`.jsonc` do opencode mesclam como objetos → ✅ Objetos (`mcp`, `lsp`) mesclam por chave, mas arrays (`plugin`) NÃO: a definição num arquivo sombreia a do outro silenciosamente (validado: `debug config` mostrava só o dcp). Chave array deve existir em UM arquivo só. **Padrão atual (2026-08-26): config ÚNICO `~/.config/opencode/opencode.json`** — `opencode.jsonc`/`tui.json` foram removidos e são auto-cleanup no provisioning; sempre validar com `opencode debug config` após editar.
- 2026-08-24 ✅ **Subagentes podem completar o trabalho e travar antes do commit** → antes de assumir que uma task falhou, rodar `git status`/`git log` (o trabalho pode estar no working tree sem commit); ao finalizar, procurar DUPLICATAS (ex.: dois IncidentBanner) e remover a não importada (DRY, zero código morto).
- 2026-08-26 ❌ Configurar o shell do OpenCode no Windows com caminho fixo (`C:\Program Files\PowerShell\7\pwsh.exe`) → ✅ Usar `"shell": "pwsh"` (resolução via PATH) (porque o PowerShell 7 via Microsoft Store instala em `WindowsApps\Microsoft.PowerShell_<versão>...` e o caminho Program Files não existe — validado na prática; o shell tool caiu no fallback).
- 2026-08-26 ❌ Podar `~/.cache/opencode/packages/` (deletar "duplicatas" @latest/plain) esperando reduzir disco → ✅ NÃO podar cache de plugins: o opencode RECRIA o working set completo referenciado no config a cada boot (validado 2x: cache voltou de 526→899 MB após cleanup) — cache ≈ working set legítimo; só reduz com menos plugins no config.
- 2026-08-26 ❌ Rodar VACUUM no `opencode.db` esperando encolher → ✅ Checar `PRAGMA freelist_count` primeiro (freelist 0 = dado 100% vivo — VACUUM não muda nada; validado: 1.26 GB incompressível). Redução real = podar sessões antigas via SQL (`part/message/event/session` por `session_id`, preservando a sessão ativa e fazendo `VACUUM`+`wal_checkpoint(TRUNCATE)` depois).
- 2026-08-26 ❌ Parsear output de `wsl.exe -l -q` (ou comandos de console Windows) com `strings.Contains` direto → ✅ Remover null bytes primeiro (`strings.ReplaceAll(out, "\x00", "")`) (porque console Windows emite UTF-16 com `\x00` intercalado — o match falha silenciosamente).
- 2026-08-26 ❌ Confiar na concordância PowerShell/Go sobre existência de diretório → ✅ Quando `Test-Path` diz True mas `os.Stat` falha, checar `Get-Item | fl LinkType,Target` (porque pode ser SYMLINK PENDENTE apontando para destino deletado — foi o caso das 33 skills firecrawl em `~/.config/opencode/skills` linkadas a `~/.agents/skills`).
- 2026-08-26 ❌ Manter plugin do opencode sem evidência de funcionamento → ✅ Validar empiricamente antes de manter: tools no toolset da sessão (goal-plugin ✓), prompt injetado (ponytail ✓), log próprio do plugin (thinking-fix: 300+ `inspect` com `isReasoningModel:false` = no-op → removido), config sem uso (models-info sem `modelsInfoUrl` = inerte → removido), UI nunca exibiu dado (visual-cache TUI → removido).
- 2026-08-29 ❌ Confiar no console Windows padrão (code page OEM 850) para renderizar Unicode → ✅ Garantir UTF-8 em 3 camadas: (1) profile PowerShell com `[Console]::OutputEncoding/InputEncoding = UTF8` + `chcp 65001`; (2) Windows Terminal profile com `commandline: pwsh -Command "chcp 65001 > $null; $env:PATH = $env:PATH"`; (3) `envctl doctor` audita o Code Page (check 12.6) (porque CP850 não mapeia emojis/glyphs Nerd Font/✔✘ → U+FFFD `�` e corrompe até logs; o fix é deployado via `envctl run shell` + reiniciar o Windows Terminal).

## Padrões / Preferências (o que funciona)

<!-- - 2026-08-20 ✅ Quando <situação>, faça <o que funciona> -->