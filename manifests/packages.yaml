# packages.yaml - Declarative System Packages, Toolchains, Volta Packages & Desktop Apps
version: "1.0"

packages:
  # ============================================================================
  # Core System Runtimes & Package Managers
  # ============================================================================
  - id: Git.Git
    name: Git SCM for Windows
    type: winget
    os: windows
    category: system
    check_command: git --version

  - id: GitHub.cli
    name: GitHub CLI
    type: winget
    os: windows
    category: system
    check_command: gh --version

  - id: Microsoft.PowerShell
    name: PowerShell 7 Core
    type: winget
    os: windows
    category: system
    check_command: pwsh --version

  - id: Docker.DockerDesktop
    name: Docker Desktop
    type: winget
    os: windows
    category: system
    check_command: docker --version

  - id: Microsoft.WSL
    name: Windows Subsystem for Linux (WSL2)
    type: winget
    os: windows
    category: system
    check_command: wsl.exe --status

  # ============================================================================
  # High-Performance CLI Utilities (Winget)
  # ============================================================================
  - id: BurntSushi.ripgrep.MSVC
    name: ripgrep (rg)
    type: winget
    os: windows
    category: cli-tools
    check_command: rg --version

  - id: sharkdp.fd
    name: fd (fast directory find)
    type: winget
    os: windows
    category: cli-tools
    check_command: fd --version

  - id: junegunn.fzf
    name: fzf (fuzzy finder)
    type: winget
    os: windows
    category: cli-tools
    check_command: fzf --version

  - id: sharkdp.bat
    name: bat (cat with syntax highlighting)
    type: winget
    os: windows
    category: cli-tools
    check_command: bat --version

  - id: astral-sh.ruff
    name: ruff (fast python linter & formatter)
    type: winget
    os: windows
    category: cli-tools
    check_command: ruff --version

  - id: astral-sh.uv
    name: uv (fast python package & project manager)
    type: winget
    os: windows
    category: cli-tools
    check_command: uv --version

  - id: dandavison.delta
    name: git-delta (syntax-highlighting pager for git)
    type: winget
    os: windows
    category: cli-tools
    check_command: delta --version

  - id: MikeFarah.yq
    name: yq (portable command-line YAML/JSON/XML processor)
    type: winget
    os: windows
    category: cli-tools
    check_command: yq --version

  # ============================================================================
  # Agent Runtime Helpers (Windows only — Linux/macOS stays isolated: uv/venv)
  # ============================================================================
  - id: Oven-sh.Bun
    name: Bun JS/TS runtime (fast agent scripts & bunx)
    type: winget
    os: windows
    category: cli-tools
    check_command: bun --version

  - id: pyyaml
    name: PyYAML (YAML parsing for python agent scripts)
    type: pip
    os: windows
    category: cli-tools
    check_command: python -m pip show pyyaml

  - id: requests
    name: requests (HTTP client for python agent scripts)
    type: pip
    os: windows
    category: cli-tools
    check_command: python -m pip show requests

  - id: openpyxl
    name: openpyxl (Excel xlsx read/write for python agent scripts)
    type: pip
    os: windows
    category: cli-tools
    check_command: python -m pip show openpyxl

  - id: beautifulsoup4
    name: beautifulsoup4 (HTML parsing for python agent scripts)
    type: pip
    os: windows
    category: cli-tools
    check_command: python -m pip show beautifulsoup4

  - id: jqlang.jq
    name: jq (JSON processor for CLI/agent pipelines)
    type: winget
    os: windows
    category: cli-tools
    check_command: jq --version

  - id: bootandy.dust
    name: dust (fast disk usage analyzer, NTFS-safe)
    type: winget
    os: windows
    category: cli-tools
    check_command: dust --version

  - id: sharkdp.hyperfine
    name: hyperfine (command benchmarking for agent evidence)
    type: winget
    os: windows
    category: cli-tools
    check_command: hyperfine --version

  - id: koalaman.shellcheck
    name: shellcheck (bash script linting for WSL/Linux scripts)
    type: winget
    os: windows
    category: cli-tools
    check_command: shellcheck --version

  # ============================================================================
  # Language SDKs & Toolchains (Winget)
  # ============================================================================
  - id: Volta.Volta
    name: Volta JS Toolchain Manager
    type: winget
    os: windows
    category: runtimes
    check_command: volta --version

  - id: GoLang.Go
    name: Go Programming Language SDK
    type: winget
    os: windows
    category: runtimes
    check_command: go version

  - id: Python.Python.3.14
    name: Python 3.14 Runtime
    type: winget
    os: windows
    category: runtimes
    check_command: python --version

  - id: Microsoft.DotNet.SDK.8
    name: .NET 8 SDK
    type: winget
    os: windows
    category: runtimes
    check_command: dotnet --version

  - id: Rustlang.Rustup
    name: Rustup Rust Toolchain
    type: winget
    os: windows
    category: runtimes
    check_command: rustup --version

  # ============================================================================
  # Volta Node.js & Global Ecosystem
  # ============================================================================
  - id: node@24.19.0
    name: Node.js LTS (via Volta)
    type: volta
    os: windows
    category: volta
    check_command: node -v

  - id: pnpm
    name: pnpm Fast Disk-Efficient Package Manager
    type: volta
    os: windows
    category: volta
    check_command: pnpm -v

  - id: firecrawl-cli
    name: Firecrawl CLI (Web Scraper & Crawler for AI)
    type: volta
    os: windows
    category: volta
    check_command: firecrawl --version

  - id: mcp-ssh-manager
    name: SSH Manager CLI & MCP Server
    type: volta
    os: windows
    category: volta
    check_command: ssh-manager --version

  - id: stylelint
    name: Stylelint CSS/SCSS Linter
    type: volta
    os: windows
    category: volta
    check_command: stylelint --version

  - id: sqllens-language-server
    name: SQL Lens Language Server Protocol
    type: volta
    os: windows
    category: volta
    check_command: sqllens-language-server --version

  - id: typescript-language-server
    name: TypeScript Language Server
    type: volta
    os: windows
    category: lsp
    check_command: typescript-language-server --version

  - id: pyright
    name: Pyright Language Server
    type: volta
    os: windows
    category: lsp
    check_command: pyright --version

  - id: bash-language-server
    name: Bash Language Server
    type: volta
    os: windows
    category: lsp
    check_command: bash-language-server --version

  - id: yaml-language-server
    name: YAML Language Server
    type: volta
    os: windows
    category: lsp
    check_command: yaml-language-server --version

  - id: dockerfile-language-server-nodejs
    name: Dockerfile Language Server
    type: volta
    os: windows
    category: lsp
    check_command: docker-langserver --version

  - id: vscode-langservers-extracted
    name: VSCode Extracted Language Servers (HTML/CSS/JSON/Markdown/ESLint)
    type: volta
    os: windows
    category: lsp
    check_command: vscode-html-language-server --version

  # ============================================================================
  # Desktop Applications & Productivity Tools (Winget)
  # ============================================================================
  - id: Microsoft.VisualStudioCode
    name: Visual Studio Code
    type: winget
    os: windows
    category: desktop-apps
    check_command: code --version

  - id: Microsoft.WindowsTerminal
    name: Windows Terminal
    type: winget
    os: windows
    category: desktop-apps

  - id: JanDeDobbeleer.OhMyPosh
    name: Oh My Posh Prompt Engine
    type: winget
    os: windows
    category: desktop-apps
    check_command: oh-my-posh --version

  - id: Termius.Termius
    name: Termius SSH Client
    type: winget
    os: windows
    category: desktop-apps

  - id: WinSCP.WinSCP
    name: WinSCP SFTP/FTP Client
    type: winget
    os: windows
    category: desktop-apps

  - id: 7zip.7zip
    name: 7-Zip Archiver
    type: winget
    os: windows
    category: desktop-apps

  - id: voidtools.Everything
    name: Everything File Search Engine
    type: winget
    os: windows
    category: desktop-apps

  - id: Brave.Brave.Nightly
    name: Brave Browser Nightly
    type: winget
    os: windows
    category: desktop-apps

  - id: GitHub.GitHubDesktop
    name: GitHub Desktop
    type: winget
    os: windows
    category: desktop-apps

  # ============================================================================
  # Global Dotnet Tools
  # ============================================================================
  - id: csharp-ls
    name: C# Language Server Protocol
    type: dotnet-tool
    os: windows
    category: lsp
    check_command: csharp-ls --version

  # ============================================================================
  # Debian / Ubuntu APT Core Packages (Linux VPS & Workstations)
  # ============================================================================
  - id: git
    name: Git SCM
    type: apt
    os: linux
    category: system
    check_command: git --version

  - id: curl
    name: cURL HTTP Client
    type: apt
    os: linux
    category: system
    check_command: curl --version

  - id: wget
    name: Wget Downloader
    type: apt
    os: linux
    category: system
    check_command: wget --version

  - id: ripgrep
    name: ripgrep (rg)
    type: apt
    os: linux
    category: cli-tools
    check_command: rg --version

  - id: fd-find
    name: fd file search
    type: apt
    os: linux
    category: cli-tools
    check_command: fdfind --version

  - id: fzf
    name: fzf fuzzy finder
    type: apt
    os: linux
    category: cli-tools
    check_command: fzf --version

  - id: bat
    name: bat cat clone
    type: apt
    os: linux
    category: cli-tools
    check_command: batcat --version

  - id: tree
    name: tree directory visualizer
    type: apt
    os: linux
    category: cli-tools
    check_command: tree --version

  - id: jq
    name: jq JSON processor
    type: apt
    os: linux
    category: cli-tools
    check_command: jq --version

  - id: unzip
    name: unzip extraction utility
    type: apt
    os: linux
    category: system
    check_command: unzip -v

  - id: zip
    name: zip compression utility
    type: apt
    os: linux
    category: system
    check_command: zip -v

  - id: rsync
    name: rsync remote file copy
    type: apt
    os: linux
    category: system
    check_command: rsync --version

  - id: build-essential
    name: GNU C/C++ Build Essentials
    type: apt
    os: linux
    category: toolchain

  - id: python3-pip
    name: Python 3 Pip
    type: apt
    os: linux
    category: toolchain
    check_command: pip3 --version

  - id: python3-venv
    name: Python 3 Virtual Environments
    type: apt
    os: linux
    category: toolchain

  - id: sshpass
    name: SSH Password Authentication Utility
    type: apt
    os: linux
    category: toolchain
    check_command: sshpass -V
