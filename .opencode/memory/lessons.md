# Agent Memory — Lessons (não repetir) — envctl

> Consulte antes de cada tarefa; atualize a cada correção/aprendizado.
> Formato: ❌ <não faça> → ✅ <faça> (porque). Entradas curtas e acionáveis.

## Erros / Lições (não repetir)

<!-- - 2026-08-20 ❌ ... → ✅ ... (porque ...) -->
- 2026-09-22 ❌ Escrever `configs/opencode*.json` no formato V1 (`agent`, `permission` com `bash`/`task`, `plugin`, `skills.paths`, `mcp` flat com `enabled`, `subagent_depth` top-level, bloco `lsp`) esperando que o opencode v2 execute tudo → ✅ Formato nativo V2 (`agents` com `system`/`permissions[]`/`request.body`, `plugins`, `skills[]`, `mcp.servers` com `disabled`+`timeout:{catalog,execution}`, `experimental.subagent_depth: 2`, `instructions[]`); `lsp` removido (runtime inerte — binários seguem provisionados p/ shell/IDE), `dcp.jsonc` removido do provisioning (YAGNI — validar sempre com `opencode debug config` após editar e checar `normalization diagnostic` no log).
- 2026-08-21 ❌ Versionar dados de servidores (IPs, usuários, caminhos de chaves) em `configs/skills/*/SKILL.md` do envctl → ✅ Manter inventário APENAS em `~/.config/opencode/extras/ssh_servers.md` (local por máquina) e referenciar dinamicamente (`ssh-manager server list`) (porque o envctl deploya as skills para todas as máquinas e o histórico git preserva PII).
- 2026-08-21 ❌ Provisionar/sincronizar memórias globais (`configs/memory/`) no envctl → ✅ (regra refinada em 2026-08-26): memórias globais SEM dados pessoais são versionadas como **templates seed** em `configs/memory/*.md` e provisionadas com `seed_if_missing: true` (escreve SÓ se o destino não existe — baseline para máquinas novas; adições locais por máquina nunca são sobrescritas nem reverse-syncadas — `snapshot_sync.go` continua bloqueando `memory/`).
- 2026-08-26 ❌ Manter config do OpenCode em DOIS arquivos (`opencode.jsonc` + `opencode.json` sobreposto) → ✅ Fonte ÚNICA `configs/opencode.json` (e `opencode.linux.json`) + seção `cleanup` no `manifests/shell.yaml` que remove `opencode.jsonc`/`tui.json` stale no provisioning (porque arrays de `plugin` não mesclam entre arquivos — o boot carregava 18 LSPs + 6 plugins; jsonc não suporta merge com json).
- 2026-08-26 ❌ Editar `configs/*` e rodar `envctl run shell` de diretório errado → ✅ Rebuild (`go build -o envctl.exe`) + rodar do root do repo (porque o `fsManager.ReadFile` lê `configs/` do CWD e cai no FS embutido ANTIGO se rodar de outro diretório — silenciosamente "Already up to date").
- 2026-08-26 ❌ Adicionar pacotes pip/novos ao manifest e instalar com binário desatualizado → ✅ Rebuild antes de `envctl run pip`/`run shell` (porque o binário embute os manifests — o run pip "Processed 2 packages" com o binário velho pulou os pacotes novos).
- 2026-08-26 ✅ [SUPERSEDED 2026-09-24] Push na main disparava Release automático (`v1.2.<commit-count>`) — removido após a poluição de 74 tags. Agora: release-please abre PR de release por conventional commits (`feat`→minor, `fix`→patch); `release.yml` só anexa binários. Nunca criar tag por push.
- 2026-08-27 ❌ Validar CLI Go na VM Linux com binário compilado no Windows → ✅ Cross-compilar `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build` (porque `go build` default gera binário do host — "Exec format error" na VM).
- 2026-08-27 ❌ Executar comandos na VM via paramiko/ssh sem login shell e esperar PATH completo → ✅ Usar `bash -lc "..."` (ou upload de script) (porque sessão não-login NÃO sourceia `.bashrc` — PATH fica vazio e volta/npm/ferramentas somem; aspas aninhadas pelo paramiko exigem script em arquivo).
- 2026-08-27 ❌ Comparar env var POSIX lida do processo com valor literal do manifest (`$HOME/...`) → ✅ rc files (`~/.bashrc`/`~/.profile`) são a fonte de verdade (porque o shell expande `$HOME` no processo herdado — comparar expandido vs literal gera WARN falso e re-escritas a cada `run shell`; fix em `env_manager.GetEnvVar`).
- 2026-08-27 ❌ Criar diretório na raiz do Linux (`/temp`) com `os.MkdirAll` como usuário comum → ✅ Fallback `sudo -n mkdir -p` + `chmod 1777` (sticky, scratch compartilhado) (porque a raiz do FS exige root — `permission denied` vira DiagError).
- 2026-08-28 ❌ Assumir que `run all`/`doctor` se comportam igual em VPS fresca e VPS antiga → ✅ VPS nova exige: (1) `apt-get update` antes do primeiro install (listas vazias → "Unable to locate package"); (2) bootstrap/Volta ANTES do passo packages (senão todo pacote volta é pulado como "manager not available"); (3) managers de toolchain resolvendo no PATH de toolchain (`~/.volta/bin` etc.) porque shell não-login (ssh/systemd) tem PATH mínimo — validado em zscanchatbot (26 WARN → 0 com os 3 fixes). Obs.: `exec.Cmd.Env` NÃO afeta a resolução do binário (`exec.LookPath` usa o PATH do processo) — resolver manualmente e passar caminho absoluto.
- 2026-08-28 ❌ Editar `~/.config/opencode/AGENTS.md` / `opencode.json` (deployados) como se fossem arquivos locais autônomos → ✅ Editar a fonte única no repo (`configs/AGENTS.md`, `configs/opencode.json`) e espelhar no arquivo deployado (ou rodar `envctl run shell`), porque o provisioning sobrescreve os globais. NUNCA colocar IPs/inventário de servidores em `configs/AGENTS.md`/skills (deployados p/ todas as máquinas — PII): inventário fica só em `~/.config/opencode/extras/ssh_servers.md` + `ssh-manager server list`.
- 2026-08-27 ❌ Versionar chaves de API em `configs/*.json` do envctl → ✅ Usar variável de arquivo do opencode `{file:~/.config/opencode/secrets/context7.key}` (porque configs são deployados para todas as máquinas e o histórico git preserva segredos; o arquivo de secrets fica local por máquina — `~/Documents/SSH-keys` e `~/.config/opencode/secrets/` NUNCA versionados).
- 2026-09-03 ❌ Confiar no profile PowerShell + check 12.6 (`chcp`) como garantia de UTF-8 no opencode → ✅ Causa raiz é o code page do SISTEMA (`HKLM\SYSTEM\CurrentControlSet\Control\Nls\CodePage` ACP/OEMCP = 1252/850 = beta UTF-8 desligado): o opencode spawna `pwsh -NoLogo -NoProfile -NonInteractive -Command` — o profile NUNCA carrega no shell tool (ENVCTL_TEMP/oh-my-posh visíveis eram herdados do ambiente do opencode.exe ou do PATH, não do profile); pwsh sem profile emite CP850 para pipe (á=0xA0, é=0x82...) e o opencode decodifica como UTF-8 → U+FFFD `�`. Fix raiz: ativar "Beta: Use Unicode UTF-8 para todo o mundo" (ACP/OEMCP=65001) + reboot; o doctor ganhou o check 12.6.1 (audita o registry) porque o `chcp` do 12.6 reflete apenas o console de quem invoca o envctl (65001 no terminal interativo mascara o problema).
- 2026-09-22 ❌ Tratar saída silenciosa de LSP (`cmdErr != nil && len(out)==0`) como "servidor mais quebrado é o mais quieto" e emitir warning → ✅ Quiet-exit É o shape saudável de stdio (node servers saem 1 com EOF saudável, calibrado nos 14 ao vivo; `TestDoctorAudit_LSPHandshakeQuietExitPasses` trava com `sh -c "exit 1"` → zero diags) — crash e saudável-quieto são indistinguíveis nessa camada, e warning ali quebra o contrato 0 WARN/0 ERRO (porque cada LSP quieto saudável viraria WARN permanente no doctor).
- 2026-09-22 ❌ `git add -A` + commit sem olhar `git status` em repo com sessões paralelas → ✅ `git status --short` ANTES de adicionar; commitar só os arquivos da própria tarefa (add por path) — um `add -A` cego sequestrou o workstream de outra sessão (remoção do bloco `lsp` + purge DCP do opencode v2) para dentro do commit de docs; split via `git reset HEAD~1` + add seletivo resolveu (porque repo compartilhado acumula mudanças fora da sua sessão).
- 2026-09-22 ❌ Duplicar o built-in `plan` do opencode num override de ~170 linhas (`description`/`request`/`steps`/`system` + allowlist shell) → ✅ Reduzir `agents.plan` à exceção mínima (`permissions: [{edit spec-agent/** allow}]`); o built-in (`opencode.plan`: primary, `question allow`, `edit * deny` + `edit ~/.opencode/plan/* allow`) cobre mode/questions/read-only, e o merge (scalars replace, `request` merge, `permissions` append) herda o resto — `debug agents` prova: mode `primary`, 45 perms, `* deny` + `~/.opencode/plan/* allow` + `spec-agent/** allow` (porque customs com IDs de built-in viram merge silencioso que apodrece a cada mudança upstream; shell allowlist fica no nível do agente, NUNCA top-level — senão o `build` herdaria `shell * ask`).
- 2026-09-22 ❌ Escrever plugin V2 de diagnóstico pós-edit p/ compensar o LSP morto (API beta, `event.subscribe` com bug de zero eventos, hook falho falha a operação) → ✅ Manter binários provisionados (`lsp.yaml` + `run lsp` + doctor como toolchain) + template per-project em `patterns.md` + monitoramento mensal (`pacman -Si opencode` + changelog upstream, assimetria #17); diagnóstico do agente via lint/typecheck (guia oficial manda esse caminho) — sem código próprio antes da API sair de beta (porque plugin beta vira dívida de manutenção a cada release do opencode).
- 2026-09-22 ❌ Provisionar via envctl tuning que exige root, reboot ou decisão de segurança (kernel cmdline, `/etc/lact/config.yaml`, `/etc/scx_loader/config.toml`, `mitigations=off`) → ✅ Só pacotes + presets user-level entram no manifesto (`gaming.yaml`, `shell.yaml` com `seed_if_missing`); tuning privilegiado vive na skill como orientação manual + `doctor` warn-only (nunca `--fix`), e `mitigations=off` fica fora de propósito (porque auto-fix em `/etc` com senha via agente gera faillock, reboot invalida verificação na mesma sessão e Spectre/Meltdown exige aprovação explícita caso a caso).
- 2026-09-24 ❌ Instalar opencode no Windows via winget + build com `go build -o envctl.exe .` na raiz → ✅ Instalador oficial PowerShell (zip de `https://opencode.ai/files/bin/<ver>/opencode-windows-x64.zip`, `-baseline` sem AVX2, extraído para `~/.local/bin`; `~/.local/bin` já está no PATH) + build com `go build -o envctl.exe ./cmd/envctl` (porque o winget congelou na linha 1.x com manifests de bot e `go build .` na raiz empacota `assets.go` sem `main`, gerando `.exe` inválido).
- 2026-09-24 ❌ Batch PowerShell com `Get-Service -Name a,b,c` + array `@( @{...}, )` com vírgula trailing → ✅ Loop por nome emitindo `NOT_PRESENT` explícito + join sem trailing (`strings.Join(entryLines, ",\n")`) (porque multi-name sai exit 1 se QUALQUER nome falta mesmo com `SilentlyContinue`, e trailing comma em `@()` é parse error no powershell 5.1 — ambos provados pelo `TestWindowsTweaksManager_CheckBatchMatchesSingle`).
- 2026-09-24 ❌ Rodar `envctl run skills`/`run shell` em máquina com sessões paralelas ativas → ✅ Checar `git status` + diff deployado-vs-manifesto antes; restaurar a própria skill com cópia aditiva (`Copy-Item`, sem prune) em vez de re-provisionar (porque o prune do `run skills` quarentena as skills da outra sessão como stale — extensão da regra `git add -A` de 2026-09-22 para o estado deployado).

## Padrões / Preferências (o que funciona)

<!-- - 2026-08-20 ✅ Quando <situação>, faça <o que funciona> -->
